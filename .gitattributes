# Normalise to LF in the repo so moving between macOS and Windows does not
# produce a CRLF diff storm.
* text=auto eol=lf

# Never touch these.
pkg/bin/** binary
*.bin binary
*.so binary
*.dll binary
