# Normalise text to LF in the repo so the working tree stays consistent
# whichever machine it is checked out on.
* text=auto eol=lf

# Never normalise these. pkg/bin/swarm_sim.exe is committed, and running an
# executable through text normalisation corrupts it.
pkg/bin/** binary
*.exe binary
*.dll binary
*.so binary
*.bin binary
