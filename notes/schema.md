# Trace schema

Filled in from `python tools/inspect_trace.py runs/fixture.jsonl --write`, which
**overwrites this whole file** with the real records. This is the input to fixing
the viewer sidecar's field mapping — until it holds real output, the viewer has
nothing correct to map.

The section headings below are a guess at the record types, kept only as a
reminder of what to look for. Replace them with whatever the counts table
actually reports.

## header

<!-- The frame column names live here. Everything else depends on getting this right. -->

## frame

## links

## beliefs

## log

## telemetry

## report

## Open questions

<!-- What is still unclear after reading the real records? -->
