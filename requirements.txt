# Python deps for the analysis/viewer sidecar. NOT needed to build or run the
# brain, and NOT needed by tools/inspect_trace.py, which is standard library
# only and deliberately stays that way.
#
# Nothing in the repo imports these yet. Install them when the viewer data
# pipeline lands:  py -m pip install -r requirements.txt
# Pin exact versions once it works.
numpy
matplotlib
